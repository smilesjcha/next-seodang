export default function ReportDetailPage({
  params,
}: {
  params: { reportId: string };
}) {
  return (
    <main>
      <h1>보고서</h1>
      {/* TODO: 보고서 에디터/뷰어 + PDF/DOCX/PPT 내보내기 */}
      {/* reportId: {params.reportId} */}
    </main>
  );
}
