export default function CollectionDetailPage({
  params,
}: {
  params: { collectionId: string };
}) {
  return (
    <main>
      <h1>컬렉션 상세</h1>
      {/* TODO: 컬렉션 내 근거 문장 리스트 */}
      {/* collectionId: {params.collectionId} */}
    </main>
  );
}
