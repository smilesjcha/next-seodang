# slides/

이 주제로 만든 **발표 자료** 누적.

## 파일 명명 규칙

- `ppt_v1_for_<자리>_<일자>.md` (예: `ppt_v1_for_yla-week12_2026-06-15.md`)
- 같은 주제를 여러 자리에서 발표하면 자리별로 다른 파일

## 워크플로

1. 해당 시점의 essays/ 최신본을 입력으로 `create-ppt(yla)` 호출
2. 출력을 본인 말투로 다듬어 저장
3. 발표 후 받은 피드백은 `essays/` 다음 버전에 반영 (slides는 그 시점의 사진처럼 보존)
